name: Build Artifact

on:
  push:
    branches:
      - main
      - master
      - release/*
      - development
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: using acheck out actions
        uses: actions/checkout@v2
      - name: Run online script
        run: hello world!
    
    
