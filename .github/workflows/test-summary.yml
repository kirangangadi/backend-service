name: Summary Test

on:
  push:
    branches: 
        - master1
  workflow_dispatch:

jobs:
  build_service_one:
    runs-on: ubuntu-latest
    name: Build Service One
    steps:
    - name: service 1
      run: |-
        echo "update the summary"
        SUMMARY=$'### Docker image created and Pushed successfully 🚀\n'
        echo "$SUMMARY Docker Image Spec: Path is Here" >> $GITHUB_STEP_SUMMARY

  
  
