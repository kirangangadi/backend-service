name: Deploy Service

on:
  push:
    branches: 
        - master1
  workflow_dispatch:
  workflow_call:

jobs:
  build_service:
    runs-on: ubuntu-latest
    name: Deploy Service 
    steps:
    - name: service 1
      run: echo "This is service 1"
